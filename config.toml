# The URL the site will be built for
base_url = "https://mixedmatched.github.io/site/"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true

theme = "anemone"

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true

[extra]
toc = true
display_author = true
footer_content_license = "🄯 2024 adrs all rites reversed"
footer_content_license_link = "/site/last-copyrites"

header_nav = [
    { url = "/site/", name_en = "/home/" },
    { url = "/site/about", name_en = "/about/" },
    { url = "/site/resume", name_en = "/resume/" },
    { url = "/site/blog", name_en = "/blog/" },
]
